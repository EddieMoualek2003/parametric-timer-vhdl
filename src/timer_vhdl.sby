[options]
# Bounded model checking is sufficient for fixed-cycle timing verification
mode bmc
depth 100

[engines]
smtbmc

[script]
plugin -i ghdl
# Analyze all 3 files, elaborate the wrapper
ghdl --std=08 timer.vhd timer.psl timer_wrapper.vhd -e timer_wrapper
prep -top timer_wrapper

[files]
src/timer.vhd
src/timer_wrapper.vhd
src/timer.psl