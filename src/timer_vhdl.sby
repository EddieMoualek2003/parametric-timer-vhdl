[options]
mode bmc
depth 50

[engines]
smtbmc

[script]
plugin -i ghdl
# Analyze both files, elaborate the wrapper
ghdl --std=08 timer.vhd timer.psl timer_wrapper.vhd -e timer_wrapper
prep -top timer_wrapper

[files]
src/timer.vhd
src/timer_wrapper.vhd
src/timer.psl